diff --git Lib/distutils/command/install.py Lib/distutils/command/install.py
index f1f3bd5..cc408df 100644
--- Lib/distutils/command/install.py
+++ Lib/distutils/command/install.py
@@ -41,8 +41,8 @@ else:
 
 INSTALL_SCHEMES = {
     'unix_prefix': {
-        'purelib': '$base/lib/python$py_version_short/site-packages',
-        'platlib': '$platbase/lib/python$py_version_short/site-packages',
+        'purelib': '$base/lib/amd64/python$py_version_short/site-packages',
+        'platlib': '$platbase/lib/amd64/python$py_version_short/site-packages',
         'headers': '$base/include/python$py_version_short/$dist_name',
         'scripts': '$base/bin',
         'data'   : '$base',
diff --git Lib/distutils/sysconfig.py Lib/distutils/sysconfig.py
index d206e0c..9b78449 100644
--- Lib/distutils/sysconfig.py
+++ Lib/distutils/sysconfig.py
@@ -115,7 +115,7 @@ def get_python_lib(plat_specific=0, standard_lib=0, prefix=None):
 
     if os.name == "posix":
         libpython = os.path.join(prefix,
-                                 "lib", "python" + get_python_version())
+                                 "lib/amd64", "python" + get_python_version())
         if standard_lib:
             return libpython
         else:
diff --git Lib/site.py Lib/site.py
index 90cf331..c1d2239 100644
--- Lib/site.py
+++ Lib/site.py
@@ -300,10 +300,10 @@ def getsitepackages():
         if sys.platform in ('os2emx', 'riscos'):
             sitepackages.append(os.path.join(prefix, "Lib", "site-packages"))
         elif os.sep == '/':
-            sitepackages.append(os.path.join(prefix, "lib",
+            sitepackages.append(os.path.join(prefix, "lib/amd64",
                                         "python" + sys.version[:3],
                                         "site-packages"))
-            sitepackages.append(os.path.join(prefix, "lib", "site-python"))
+            sitepackages.append(os.path.join(prefix, "lib/amd64", "site-python"))
         else:
             sitepackages.append(prefix)
             sitepackages.append(os.path.join(prefix, "lib", "site-packages"))
diff --git Lib/sysconfig.py Lib/sysconfig.py
index 9e8acc5..2ac5f8b 100644
--- Lib/sysconfig.py
+++ Lib/sysconfig.py
@@ -7,10 +7,10 @@ from os.path import pardir, realpath
 
 _INSTALL_SCHEMES = {
     'posix_prefix': {
-        'stdlib': '{base}/lib/python{py_version_short}',
-        'platstdlib': '{platbase}/lib/python{py_version_short}',
-        'purelib': '{base}/lib/python{py_version_short}/site-packages',
-        'platlib': '{platbase}/lib/python{py_version_short}/site-packages',
+        'stdlib': '{base}/lib/amd64/python{py_version_short}',
+        'platstdlib': '{platbase}/lib/amd64/python{py_version_short}',
+        'purelib': '{base}/lib/amd64/python{py_version_short}/site-packages',
+        'platlib': '{platbase}/lib/amd64/python{py_version_short}/site-packages',
         'include': '{base}/include/python{py_version_short}',
         'platinclude': '{platbase}/include/python{py_version_short}',
         'scripts': '{base}/bin',
diff --git Lib/test/test_site.py Lib/test/test_site.py
index 1988ef3..dc42ac3 100644
--- Lib/test/test_site.py
+++ Lib/test/test_site.py
@@ -228,10 +228,10 @@ class HelperFunctionsTests(unittest.TestCase):
             self.assertEqual(dirs[0], wanted)
         elif os.sep == '/':
             self.assertEqual(len(dirs), 2)
-            wanted = os.path.join('xoxo', 'lib', 'python' + sys.version[:3],
+            wanted = os.path.join('xoxo', 'lib/amd64', 'python' + sys.version[:3],
                                   'site-packages')
             self.assertEqual(dirs[0], wanted)
-            wanted = os.path.join('xoxo', 'lib', 'site-python')
+            wanted = os.path.join('xoxo', 'lib/amd64', 'site-python')
             self.assertEqual(dirs[1], wanted)
         else:
             self.assertEqual(len(dirs), 2)
diff --git Makefile.pre.in Makefile.pre.in
index ed0d116..c4dbb2d 100644
--- Makefile.pre.in
+++ Makefile.pre.in
@@ -97,7 +97,7 @@ LIBDIR=		@libdir@
 MANDIR=		@mandir@
 INCLUDEDIR=	@includedir@
 CONFINCLUDEDIR=	$(exec_prefix)/include
-SCRIPTDIR=	$(prefix)/lib
+SCRIPTDIR=	$(prefix)/lib/amd64
 
 # Detailed destination directories
 BINLIBDEST=	$(LIBDIR)/python$(VERSION)
diff --git Modules/Setup.dist Modules/Setup.dist
index e02fd77..a02a936 100644
--- Modules/Setup.dist
+++ Modules/Setup.dist
@@ -460,7 +460,7 @@ GLHACK=-Dclear=__GLclear
 # Andrew Kuchling's zlib module.
 # This require zlib 1.1.3 (or later).
 # See http://www.gzip.org/zlib/
-#zlib zlibmodule.c -I$(prefix)/include -L$(exec_prefix)/lib -lz
+#zlib zlibmodule.c -I$(prefix)/include -L$(exec_prefix)/lib/amd64 -lz
 
 # Interface to the Expat XML parser
 #
diff --git Modules/getpath.c Modules/getpath.c
index 9faafa3..cb79998 100644
--- Modules/getpath.c
+++ Modules/getpath.c
@@ -129,7 +129,7 @@ static char prefix[MAXPATHLEN+1];
 static char exec_prefix[MAXPATHLEN+1];
 static char progpath[MAXPATHLEN+1];
 static char *module_search_path = NULL;
-static char lib_python[] = "lib/python" VERSION;
+static char lib_python[] = "lib/amd64/python" VERSION;
 
 static void
 reduce(char *dir)
@@ -525,10 +525,13 @@ calculate_path(void)
     if (pfound > 0) { /* Use the reduced prefix returned by Py_GetPrefix() */
         reduce(zip_path);
         reduce(zip_path);
+        reduce(zip_path);
     }
     else
         strncpy(zip_path, PREFIX, MAXPATHLEN);
-    joinpath(zip_path, "lib/python00.zip");
+    
+    /* Add amd64 directory */
+    joinpath(zip_path, "lib/amd64/python00.zip");
     bufsz = strlen(zip_path);   /* Replace "00" with version */
     zip_path[bufsz - 6] = VERSION[0];
     zip_path[bufsz - 5] = VERSION[2];
@@ -637,6 +640,8 @@ calculate_path(void)
     if (pfound > 0) {
         reduce(prefix);
         reduce(prefix);
+	/* amd64 library path ajust */
+        reduce(prefix);
         /* The prefix is the root directory, but reduce() chopped
          * off the "/". */
         if (!prefix[0])
@@ -649,6 +654,8 @@ calculate_path(void)
         reduce(exec_prefix);
         reduce(exec_prefix);
         reduce(exec_prefix);
+	/* amd64 library path ajust */
+        reduce(exec_prefix);
         if (!exec_prefix[0])
                 strcpy(exec_prefix, separator);
     }
diff --git setup.py setup.py
index 6b47451..994af8b 100644
--- setup.py
+++ setup.py
@@ -427,7 +427,7 @@ class PyBuildExt(build_ext):
         # if a file is found in one of those directories, it can
         # be assumed that no additional -I,-L directives are needed.
         lib_dirs = self.compiler.library_dirs + [
-            '/lib64', '/usr/lib64',
+            '/usr/lib/amd64/',
             '/lib', '/usr/lib',
             ]
         inc_dirs = self.compiler.include_dirs + ['/usr/include']
@@ -677,11 +677,11 @@ class PyBuildExt(build_ext):
             elif curses_library:
                 readline_libs.append(curses_library)
             elif self.compiler.find_library_file(lib_dirs +
-                                                     ['/usr/lib/termcap'],
+                                                     ['/usr/lib/amd64/termcap'],
                                                      'termcap'):
                 readline_libs.append('termcap')
             exts.append( Extension('readline', ['readline.c'],
-                                   library_dirs=['/usr/lib/termcap'],
+                                   library_dirs=['/usr/lib/amd64/termcap'],
                                    extra_link_args=readline_extra_link_args,
                                    libraries=readline_libs) )
         else:
